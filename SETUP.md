# 🚀 دليل الإعداد والتثبيت

## متطلبات النظام المسبقة

- **Node.js**: الإصدار 16 أو أعلى
- **npm**: الإصدار 8 أو أعلى (أو `yarn`)
- **متصفح حديث**: Chrome, Firefox, Safari, Edge

## خطوات التثبيت والتشغيل

### 1️⃣ استنساخ المستودع

```bash
git clone https://github.com/boki-sudo/green-volunteer-ambassador.git
cd green-volunteer-ambassador
```

### 2️⃣ تثبيت المكتبات

```bash
npm install
```

أو باستخدام yarn:

```bash
yarn install
```

### 3️⃣ تشغيل خادم التطوير

```bash
npm run dev
```

سيفتح المتصفح تلقائياً على `http://localhost:3000`

### 4️⃣ بناء للإنتاج

```bash
npm run build
```

ستجد الملفات المُترجَمة في مجلد `dist/`

### 5️⃣ معاينة الإنتاج

```bash
npm run preview
```

## 📱 اختبار الأدوار المختلفة

### دور المتطوع 🧑‍🎓
- انقر على زر **"شاب متطوع"** في رأس الصفحة
- استكشف المهام المتاحة
- جرّب تحميل الصور وتقديم التقارير
- تصفح لوحة الصدارة والشارات

### دور المشرف 🧑‍💼
- انقر على زر **"مشرف مؤسسة"** في رأس الصفحة
- استعرض قائمة التوثيقات المعلقة
- وافق على أو ارفض التوثيقات
- أنشئ مهام جديدة للمتطوعين

## 🎨 تخصيص الألوان والأنماط

تعديل الألوان في ملف `tailwind.config.js`:

```javascript
extend: {
  colors: {
    emerald: { /* ألوان أخضر مخصصة */ },
  }
}
```

## 🌍 تغيير اللغة والاتجاه

البيانات الوهمية الحالية بالعربية كاملة مع دعم RTL:
- ملف RTL موجود في `src/index.css`
- جميع المكونات تدعم الاتجاه من اليمين إلى اليسار

## 🐛 استكشاف الأخطاء

### المشكلة: المكونات لا تظهر بشكل صحيح

**الحل:**
```bash
npm install
npm run dev
```

### المشكلة: أخطاء في بناء Tailwind

**الحل:**
```bash
rm node_modules
npm install
npm run dev
```

### المشكلة: المنفذ 3000 قيد الاستخدام

**الحل:** عدّل `vite.config.js`:
```javascript
server: {
  port: 3001, // أو أي منفذ آخر
}
```

## 📦 الملفات الهامة

| الملف | الوصف |
|------|-------|
| `src/App.jsx` | المكون الرئيسي |
| `src/components/` | جميع مكونات المستخدم |
| `src/data/mockData.js` | البيانات الوهمية |
| `tailwind.config.js` | تكوين Tailwind |
| `package.json` | المكتبات والبرامج |

## 🚀 نصائح الإنتاج

### تحسين الأداء

1. **تقليل حجم الحزمة:**
   ```bash
   npm run build
   ```

2. **فحص حجم الملفات:**
   ```bash
   npm install -g vite-plugin-visualizer
   ```

3. **استخدام CDN للصور:**
   - استبدل URLs الصور بروابط CDN

### الأمان

- تأكد من إزالة بيانات التصحيح من `mockData.js` في الإنتاج
- لا تخزن مفاتيح API حساسة في الكود الأمامي

## 🤝 المساهمة

1. أنشئ فرع جديد:
   ```bash
   git checkout -b feature/اسم-الميزة
   ```

2. اجعل تغييراتك وأرسلها:
   ```bash
   git add .
   git commit -m "إضافة: وصف الميزة الجديدة"
   git push origin feature/اسم-الميزة
   ```

3. افتح Pull Request 🎉

## 📝 ملاحظات مهمة

- البيانات الحالية **وهمية فقط** للعرض التوضيحي
- في الإنتاج، ستحتاج إلى API حقيقي للبيانات
- جميع الصور الحالية من Unsplash للأغراض التوضيحية فقط
- شهادات التطوع محاكاة فقط (لا تُنشئ PDFs حقيقية حالياً)

## 🔗 الموارد المفيدة

- [React Documentation](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com)
- [Vite Guide](https://vitejs.dev)
- [Lucide Icons](https://lucide.dev)

---

**هل تواجه مشكلة؟** يرجى فتح issue على GitHub! 📝
